<table>
  <tr>
    <th>
      <h2>Students</h2>
    </td>
  </tr>
  <% @students.each do |s| %>
    <tr>
      <td>
        <a href="/students/<%= s.id %>"><%= s.first_name %>
          <%= s.last_name %></a>
      </td>
    </tr>


  <% end %>
      <a href="/students/new/">Create a new Student</a>
</table>
