<table>
    <tr>
        <th>Students</td>
    </tr>
    <% @students.each do |s| %>
    <tr>
        <td><a href="/students/<%= s.id %>"><%= s.first_name %> <%= s.last_name %></a></td>
    </tr>
    <% end %>
 </tr>
