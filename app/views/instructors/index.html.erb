<table>
  <tr>
    <th>
      <h4>Instructors</h4>
    </td>
  </tr>
  <% @instructors.each do |instructor| %>
    <tr>
      <td>
        <a href="/instructors/<%= instructor.id %>"><%= instructor.first_name %>
          <%= instructor.last_name %></a>
      </td>
    </tr>

  <% end %>
  <a href="/instructors/new/">Create a new Instructor</a>
</table>
</a>
