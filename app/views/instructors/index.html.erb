 
<table>
    <tr>
        <th>Instructors</td>
    </tr>
    <% @instructors.each do |s| %>
    <tr>
        <td><a href="/instructors/<%= s.id %>"><%= s.first_name %> <%= s.last_name %></a></td>
    </tr>
    <% end %>
 </table>