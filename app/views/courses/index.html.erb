 
<table>
    <tr>
        <th>Courses</td>
    </tr>
    <% @courses.each do |s| %>
    <tr>
        <td><a href="/courses/<%= s.id %>"><%= s.first_name %> <%= s.last_name %></a></td>
    </tr>
    <% end %>
 </table>